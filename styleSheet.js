var React = require("react-native");

var myStyles = React.StyleSheet.create({
  text: { color: "red" },
  container: {
    display: "flex",
    alignItems: "center"
  }
});

module.exports = myStyles;
